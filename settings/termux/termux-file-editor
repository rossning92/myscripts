#!/data/data/com.termux/files/usr/bin/env sh

# Termux passes the local path of every received file as the first argument.
# Route it through the same menu regardless of its MIME type or extension.
exec "$HOME/myscripts/bin/run_script" ext/contextmenu.py "$1"
